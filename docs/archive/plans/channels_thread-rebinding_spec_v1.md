# channels: one Discord thread across a supervisor's restarts, v1

Status: Shelved
Commit Model: Branch-and-PR. The work ran on branch `rebind-live-proof`, which is deleted; see the close-out Chapter for what survives and where.
Created: 2026-09-11
Worker: the `agent_persona` supervisor's own persona, building this on the operator's explicit instruction (relayed via `agent_persona/discussion.md`) that this repository is open for this one requirement.

## Goal

A long-running Claude Code supervisor (`agent_persona`'s `bin/supervise.sh`) restarts its child on every goal completion, and on a crash. Each restart is a new Claude Code session with a new session ID. Today the broker binds a Discord thread to a session ID (`broker/registry.ts`), so a supervisor's restart opens a brand-new thread, and the operator ends up following a trail of short-lived threads instead of one conversation with one long-running worker.

The fix: a session that carries a stable "lineage name" (set by the supervisor, constant across every child it launches) rebinds to the thread its lineage already owns, instead of opening a new one. The existing per-session behavior for every other launch shape (the wrapper, `wrapper/Enter-ClaudeSession.ps1`) is unchanged: only a session that opts into a lineage name gets this behavior, and the impostor guard at `registry.ts:484` still holds for every session that doesn't.

## Related plans

[`sapplefeld-channels_dsh-bridge_spec_v1.md`](sapplefeld-channels_dsh-bridge_spec_v1.md) is superseded by the `agent_persona` supervisor and archived beside this plan. This plan never touched `bridge/` and had no dependency on it.

[`channels_lineage-takeover-credential_spec_v1.md`](channels_lineage-takeover-credential_spec_v1.md) is the remedy plan this plan's finishing pass produced for the lineage takeover's missing credential. It is archived unbuilt, declined on the same ruling that shelved this plan.

## Roadmap

1. A session declares its lineage. A new env var, `CHANNEL_LINEAGE` (parallel to `CHANNEL_SESSION` and `CHANNEL_PROCESS_TOKEN`), set by a launcher that wants restart continuity. `bin/supervise.sh` in `agent_persona` sets it once per supervisor lifetime (already has the stable name available as `$CHANNEL_NAME`); the wrapper (`Enter-ClaudeSession.ps1`) never sets it, so interactive wrapper-launched sessions are unaffected. Proof: a registry unit test asserting a session with no `CHANNEL_LINEAGE` behaves exactly as today.

2. The registry rebinds instead of creating. When a session with a `CHANNEL_LINEAGE` value registers (`SessionStart` hook post, `POST /hook`), the registry checks its persisted thread bindings for an existing thread bound to that lineage. If one exists, the new session's record takes over that thread binding (the old session's own record is superseded the way a same-token restart already supersedes today, per `registry.ts`'s existing "ends a previous session when a token announces a new one" behavior) rather than the surface creating a new thread. If none exists (the supervisor's first-ever launch), a new thread is created and bound to the lineage, same as today's per-session binding, so the lineage becomes the thread's owner from then on. Proof: a registry unit test where a second session registers with the same `CHANNEL_LINEAGE` as a first, asserting the thread ID returned to the second session equals the first session's thread ID, and that the first session's own binding record is superseded (no longer the active binding for that thread) exactly as a same-token restart supersedes today.

3. A restart says so, once, in the thread. The moment a lineage rebind happens (not the first launch), the surface posts one line into the thread - a system-style notice, not attributed to either party - naming that the supervisor restarted. This uses the existing notice-posting path the rest of the surface already has (the `Discord outbound` reconciliation pass in `docs/architecture.md`), not a new one. Proof: a live run - launch a session with a lineage, note its thread, kill it, launch a second session with the same lineage - observed directly in the real Discord thread as one continuous conversation with a single restart notice line in it, not two threads.

4. The impostor guard is unaffected. `registry.ts:484` and its neighbors, which guard the title-storage cycle, take no new input from this feature and are not touched. A session carrying no lineage is invisible to the new lookup path entirely - it is a strict addition, not a rewrite of the existing binding logic.

5. `docs/architecture.md` gets one paragraph. Under "The name a session goes by" or its own small section: what a lineage is, that it is opt-in, and that it is the one case where a restart's new session ID does not mean a new thread. Proof: the paragraph is in place and a fresh read of the document (as the blind-reader review this repo's own docs hold themselves to) does not need this plan doc to understand what a lineage is.

6. The lineage takeover's interim guard. Ratified by the operator on 2026-09-20 as a finishing-pass amendment, after the finishing reviews found the takeover carried no credential and any local process could claim a lineage's thread. The guard is interim and is not a credential: the full remedy is `channels_lineage-takeover-credential_spec_v1.md`. This item exists because the guard shipped with no acceptance criterion anywhere in this plan, which left every review free to grade its description against a threat model of the reviewer's own.

   What it guarantees:

   - G1. A lineage takeover is refused while the incumbent session's process token holds a relay pipe, either attached or inside the relay hub's reconnect grace (`broker/discord/surface.ts`'s takeover loop, `liveRelayCheck` in `broker/index.ts`, `attachedOrPending` in `broker/routing/relays.ts`).
   - G2. A claimant registering on the incumbent's own process token is exempt, so a `/clear` succeeds itself on the same pipe rather than being refused its own thread.
   - G3. A claimant whose own state is exited takes nothing over.
   - G4. A takeover preserves the thread's sticky title, so a rename survives a restart.
   - G5. A takeover, a refusal and the restart notice each log. A refusal logs once per incumbent-and-claimant pair rather than once per pass. No process token is logged: the surface holds no reference to one anywhere in the file, so it cannot.

   What it explicitly does not claim, which `docs/security-model.md` states as the residual:

   - N1. It does not stop a claim made while no pipe is held for the incumbent's token. That is the window between a worker's kill and its replacement registering, and the window after a broker restart until each relay reconnects. The reconnect grace narrows neither, because the hub's pending map is built empty and nothing restores it across a restart. Leaving this window open rather than adding a post-restart hold is the operator's decision of 2026-09-20.
   - N2. It does not stop a process holding the incumbent's `CHANNEL_PROCESS_TOKEN`, which every subprocess of a session inherits. In this model the token is already the session's identity, and a same-token registration under any source but `startup` already replaces the live record without involving a lineage at all. Accepted, not fixed.
   - N3. It authenticates nothing about a claimant. Only the credential plan does that.

   Proof: the liveness tests in `broker/index.test.ts` and `broker/routing/relays.test.ts`, plus a conformance read of `docs/security-model.md` against G1 to G5 and N1 to N3 that finds no claim outside that list.

## Gate

This repository's own: `npm run lint` then `npm test`, both exit codes read from the run, not grepped from their output. No live-suite equivalent exists in this repository the way `agent_persona`'s `.kit/live-all.sh` does; the roadmap's own proof lines (registry unit tests, the live restart-cycle observation) are the acceptance evidence beyond the gate.

## Out of Scope

- Multiple supervisors sharing one lineage name (a collision is a caller error the registry may refuse or log, not something this plan resolves).
- Anything under `bridge/` (see Related plans).
- Changing the wrapper's own per-session behavior.

## Chapters

### Chapter 1 - 2026-09-12
Completed: items 1-5 (CHANNEL_LINEAGE, the rebind, the restart notice, the impostor guard confirmed unaffected, the architecture.md paragraph)
Implemented By: main session (`agent_persona`'s supervisor persona), across four commits: `c05c63a` (item 1), `0bcf3f2` (item 2), `a637bff` (item 3), and this Chapter's own commit (item 4's confirmation and item 5's paragraph)
Metrics: review rounds 0 inline (no dispatch this section); provenance 5 spec-traceable (the roadmap's own five items), 0 fix-introduced, 0 new-requirement; rulings 0 refused, 0 declared, 0 asked; NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: Item 1 is purely additive - every existing HookIntake/SessionRecord/SessionView literal across the test suite got `lineage: null`, which is item 1's own "behaves exactly as today" proof lived out in every one of them rather than argued separately. Item 2's real discovery: the surface reconciler (`broker/discord/surface.ts`) is documented as never posting a Discord message itself - "the surfaces reconcile passive state on a timer and must never post" (`ThreadMessenger`'s own comment) - which the roadmap's own wording ("this uses the existing notice-posting path the rest of the surface already has") did not anticipate needing a second call site. `onRebind` is the signal `entryFor` emits; `broker/index.ts`, the one place `Surface` and `ThreadMessenger` already meet, is where the actual `postToThread` call for item 3 lives. Item 2 carries two defensive guards the roadmap text does not spell out: a lineage match against an entry with no real thread yet (`messageId === null`) is skipped, since there is nothing to take over, and a match against an entry Discord has already permanently refused (`abandoned`) is skipped too, since resurrecting a broken surface silently is worse than a fresh thread. Item 4 needed no code: `impostorStart`/`impostor` in `broker/registry.ts` read only `sessionId` and `processToken`, confirmed by reading the function directly rather than trusting the roadmap's own claim.
Assumptions: The restart notice's wording ("↻ supervisor restarted · <lineage>") and its treatment as a new attribution class registered in `ATTRIBUTION_OPENERS` (the spoofing guard every other notice line already uses) are this session's own calls, following the file's established pattern rather than a new one (2026-09-12).
Review Findings: none yet (posted for the Reviewer's next round on `agent_persona`'s DISCUSSION.md)
Stamps: none - this repository carries no live-suite equivalent; see the plan's own Gate section
Gate: this repository's own - `npm run lint` (`tsc --noEmit`) exit 0, `npm test` exit 0, 1711 of 1712 pass, 1 pre-existing skip, 0 fail, at each of the four commits in this Chapter (baseline before item 1 was 1704 of 1705 pass).
Next: the live restart-cycle proof (items 2 and 3's own proof lines) - launch a session with a lineage, note its thread, kill it, launch a second under the same lineage, observe one continuous thread with a restart notice in it, not two threads. This needs a real Discord-connected broker and cannot be proven by this repository's own test suite.
Commit Model: Branch-and-PR

### Chapter 2 - 2026-09-12
Completed: item 2's own defect, found by the Reviewer's live probe against the running broker rather than by reading - a departed session's roster record keeps arriving in every later tick, so a rebind flip-flopped the thread between the old and new session ID on each pass.
Implemented By: main session (`agent_persona`'s supervisor persona)
Metrics: review rounds 1 (Reviewer's live-broker probe, Round 60); provenance 1 review-finding, 0 fix-introduced, 0 new-requirement; rulings 0 refused, 1 declared (in-memory rather than persisted supersession, see Assumptions), 0 asked; NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: the broker's registry never drops an ended session's record, so `entryFor`'s lineage-match loop saw that session's own next view as a brand-new session under its old lineage - the entry the takeover had just moved away was gone from `threads`, so nothing distinguished "never registered" from "just superseded." Fixed with a `Set<string>` of superseded session IDs, populated at the takeover site and checked at the top of `reconcile`: a superseded session's own view builds nothing, ever, no matter how many more ticks the roster reports it (ended or still claiming live - the Reviewer's probe hit both shapes and the fix does not distinguish them).
Assumptions (amended - see Chapter 3): this Chapter shipped with the superseded set held in memory only, not persisted alongside the thread bindings, on the stated assumption that a broker restart was out of scope. The Reviewer named that assumption itself as the one blocking item left on item 2 (Round 62 point 3): a broker restart is not out of scope for a feature whose whole purpose is surviving restarts, and the live proof is itself a broker restart. Chapter 3 replaces the in-memory set with a structural ordering fix that needs no new persisted state at all.
Review Findings: Round 60 point 3 (this Chapter). Reviewer's own live probe: three ticks, five `onRebind` events alternating `a->b`/`b->a`, `threadFor("session-a")` landing on `null` or `thread-1` depending on tick parity.
Stamps: none - this repository carries no live-suite equivalent; see the plan's own Gate section
Gate: this repository's own - `npm run lint` (`tsc --noEmit`) exit 0, `npm test` exit 0, 1713 of 1714 pass, 1 pre-existing skip, 0 fail (baseline before this Chapter was 1711 of 1712 pass). Fix proven red-then-green: reverted the `reconcile` guard, confirmed 2 of the 2 new tests failed (exit 1), restored from a pre-probe backup verified byte-identical via `diff`, reran green.
Next: the live restart-cycle proof (items 2 and 3's own proof lines), still held pending the operator's answer on restarting the one live broker (127.0.0.1:8787, serving both active Discord threads) onto this branch's code.
Commit Model: Branch-and-PR

### Chapter 3 - 2026-09-12
Completed: item 2's broker-restart gap (Round 62 point 3) - the in-memory superseded set from Chapter 2 could not survive a broker restart, and the live proof this whole feature is for is a broker restart.
Implemented By: main session (`agent_persona`'s supervisor persona)
Metrics: review rounds 1 (Reviewer, Round 62); provenance 1 review-finding, 0 fix-introduced, 0 new-requirement; rulings 0 refused, 0 declared, 0 asked (the Reviewer's own two acceptable shapes named a preference, taken directly); NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: took the Reviewer's preferred shape - `startedAt` (already on `SessionRecord`, now carried onto `SessionView`) orders a takeover so it only ever runs from an older session to a newer one, which makes the wrong direction structurally impossible rather than remembered-and-checked, and needs no new persisted state (the registry's own persistence already reloads `startedAt` on every broker restart). The superseded set is gone entirely. One thing the ordering guard alone did not cover, found while proving the broker-restart test: `entryFor`'s takeover moves a thread's binding to a new session ID inside the in-memory `threads` map, but nothing had called `bound()` to persist that key change unless some unrelated card/name state happened to change in the same pass - a real broker restart between the takeover and any such incidental change would restore the binding under the *old* session ID, defeating the whole fix. Added an unconditional `bound()` call at the takeover site itself. `entryFor`'s return type widened to `ThreadState | null`: null is the new "a same-or-newer session already holds this lineage's thread, build nothing" signal, replacing what the superseded set used to enforce from the reconcile side.
Assumptions: none beyond what Chapter 2 already declared and this Chapter now closes (2026-09-12).
Review Findings: Round 62 point 3 (this Chapter).
Stamps: none - this repository carries no live-suite equivalent; see the plan's own Gate section
Gate: this repository's own - `npm run lint` (`tsc --noEmit`) exit 0, `npm test` exit 0, 1714 of 1715 pass, 1 pre-existing skip, 0 fail (baseline before this Chapter was 1713 of 1714 pass). Fix proven red-then-green: reverted the ordering guard, confirmed 2 of the 3 rebind-holds tests failed (exit 1), restored from a pre-probe backup verified byte-identical via `diff`, reran green. A fourth test rebuilds a second surface from the first one's own `onBind` output (what a real broker restart restores from disk) and asserts zero further rebinds across three ticks.
Next: the live restart-cycle proof (items 2 and 3's own proof lines), still held pending the operator's answer on restarting the one live broker (127.0.0.1:8787, serving both active Discord threads) onto this branch's code.
Commit Model: Branch-and-PR

### Chapter 4 - 2026-09-12
Completed: item 2's last blocking case (Round 64 point 2) - the supervisor's actual hard-kill path, where the old session's roster record stays live and reconciles before the new one's after a broker restart, still opened a second thread.
Implemented By: main session (`agent_persona`'s supervisor persona)
Metrics: review rounds 1 (Reviewer, running the restart shape four ways, Round 64); provenance 1 review-finding, 0 fix-introduced, 0 new-requirement; rulings 0 refused, 0 declared, 0 asked; NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: Chapter 3's `startedAt` ordering only helps once an entry carries real lineage and startedAt, and a restored placeholder (a binding brought back before its own session has re-registered) carried neither - null lineage, until then. Three of the four restart-shape combinations survived anyway (an ended-view fresh entry is born abandoned, which happens to block the duplicate independently), but a still-live old session reconciling first fell through the lineage-mismatch to a fresh, non-abandoned entry: a second thread. Fixed by persisting `lineage` and `startedAt` on `ThreadBinding` (both widened for a snapshot predating the fields, defaulting to `null`/`0`) and threading them through `placeholder()`, so a restored entry can join the match from the first pass, before its session has said anything. Caught one more gap while writing this Chapter's own test: the takeover's `bound()` call (added in Chapter 3) fired before `entry.lastView` was updated to the new session's view, so the very first persisted binding after a takeover still carried the *old* session's lineage and startedAt - reordered the assignment ahead of `bound()`.
Assumptions: none (2026-09-12).
Review Findings: Round 64 point 2 (this Chapter).
Stamps: none - this repository carries no live-suite equivalent; see the plan's own Gate section
Gate: this repository's own - `npm run lint` (`tsc --noEmit`) exit 0, `npm test` exit 0, 1715 of 1716 pass, 1 pre-existing skip, 0 fail (baseline before this Chapter was 1714 of 1715 pass). Fix proven red-then-green: reverted `placeholder()`'s lineage/startedAt back to null/0, confirmed the new test failed (exit 1), restored from a pre-probe backup verified byte-identical via `diff`, reran green.
Next: the live restart-cycle proof (items 2 and 3's own proof lines), still held pending the operator's answer.
Commit Model: Branch-and-PR

### Chapter 5 - 2026-09-20
Completed: the live restart-cycle proof (items 2 and 3's own proof lines), held since Chapter 1 pending an operator answer on restarting the one live broker onto this branch's code. That answer turned out not to be needed, and the proof was already sitting in production.
Implemented By: main session (the passive supervisor, lineage `DEV-DISCORD`)
Metrics: review rounds 0 (this section changed no code; its whole delta is this Chapter); provenance 2 spec-traceable (items 2 and 3's own proof lines), 0 fix-introduced, 0 new-requirement; rulings 0 refused, 0 declared, 0 asked; NEEDS_CONTEXT count 0; escalations 0; consults 0
Decisions / Surprises: the held question was moot before it was asked. The live broker is PID 3160, `node D:\discord-channels\broker\index.ts`, started 2026-09-20T04:34:42Z; that checkout's `broker/` sources were last written 2026-09-13T20:36:03Z, when it fast-forwarded to `1d406c6`, this plan's own merged PR. So the running process already carried the rebind code, and no restart-onto-branch-code was owed. The proof itself is read out of the persisted bindings at `%LOCALAPPDATA%\sapplefeld-channels\discord-threads.json` against Discord's snowflake epoch, which encodes each thread's creation time in its ID. Four lineages (`STEWARD`, `ARCHITECT`, `DEV-DISCORD`, `DEV-PLUGIN`) are bound to threads created 04:02:15Z through 04:02:21Z while the sessions now holding them started 04:35:37Z through 04:35:40Z: each thread is roughly 33 minutes older than the session that owns it. That is item 2's own acceptance ("the thread ID returned to the second session equals the first session's thread ID") observed in production rather than in a unit test, and because the broker itself came up at 04:34:44Z between the two, it is Chapters 3 and 4's broker-restart case as well, restored from disk and rebound on the first pass. Across all 12 bindings no lineage owns two threads, which is the negative half of item 2 and the exact shape a failed rebind would have left. Three sessions did open fresh threads in that window (`coordinator`, `supervisor-dev`, and an `AP: Expert` session), and the log names the cause on each: `HTTP 404 (10008: Unknown Message)` on their card edits, so those starter messages were gone from Discord and a new thread was the right answer. One of the three carries no lineage at all, which is what rules this feature out as the cause rather than an argument that it behaved.
Assumptions: none (2026-09-20).
Review Findings: no reviewer pair dispatched - this section changed no code, so step 3's trivial-section clause leaves the pair optional and finishing-work's whole-changeset pass still covers the delta. blind: no code diff. One acceptance clause is recorded unproven rather than met: item 3's restart notice line could not be confirmed from this seat. `onRebind` (`broker/index.ts:1152`) posts the notice and logs nothing, so the broker log carries no record of it in either direction. A grep for `rebind` and `restarted` over the live log returns zero against a control of 2,591 `session` hits in 36,426 lines, which establishes that the handler is silent, never that no rebind occurred. Reading the line itself needs a human looking at the Discord thread.
Stamps: adjudicated 27 unstamped operator-tier records over the 8d span since Chapter 4, stamped 1 (`unstamped-lists-peer-session-reads`, which is itself what ruled the other 26 out as peer-session reads rather than this section's own).
Gate: this repository's own - `npm run lint` (`tsc --noEmit`) exit 0, `npm test` exit 0, 1717 tests, 1716 pass, 1 pre-existing skip, 0 fail, each exit code read from the run itself at `93f2c6d` with the worktree clean but for this Chapter. Chapter 4's baseline was 1715 of 1716 pass with 0 fail, so the one added test arrives with the merged boot-task work (#2) and not with this section; 0 failing then, 0 failing now. Contention lane: the process list was polled before the run and carried no foreign test runner or build, and the machine's heavy-process claim was taken for the run and released after it. This checkout had no `node_modules`, so `npm ci` ran first (exit 0).
Next: finishing-work.
Commit Model: Branch-and-PR
Delta: read at 2026-09-20T04:46Z on SCOTT-CLAUDE, worktree against `93f2c6d`, no foreign contention observed.
```
kit-size: measured no file at all under the measured roots, no tracked path a root holds was absent from the pathspec-filtered listing, and no untracked file a measured shape reaches was found either, so the corpus is empty rather than hidden and there is no reading to report
```

### Interim board 1 - 2026-09-20
Pass: finishing-work over this plan, steps 1 through 4 complete and a fix round applied. No section closed, so this is a board entry rather than a Chapter.
Scope decision: the finishing reviews ran over the whole merged feature changeset (`c40fbd5..90f1fb4`, 28 files, 593 insertions) rather than over this branch's one-Chapter diff. The strict base ref under Branch-and-PR is the merge-base `93f2c6d`, whose diff is Chapter 5 alone. The feature shipped five items and merged PR #1 without any finishing pass ever running, so the whole changeset is the honest unit. The scope-adjudicator was asked whether that call was right and ruled it was.
Gate integrity: every review ran at its assigned tier, read from each transcript's own assistant lines rather than from the requested-model sidecar. security-reviewer 37/37 claude-fable-5-1; adversarial-reviewer 43/43 claude-fable-5-1; scope-adjudicator 25/25 claude-fable-5-1; consultant 56/56 claude-fable-5-1; qa-verifier 44/44 claude-sonnet-5, which its own charter pins. No substitution, no compensation owed. The implementer ran 116/116 claude-opus-5, inherited, so the operator's mid-run Fable limit orphaned nothing.
QA: PASS. Items 1, 2, 4 and 5 confirmed by direct code and test inspection. Item 3 recorded operator-only: the notice handler writes no log, so no local artifact can confirm the live post.
Goal read: RULED. Three built-but-unasked items, all accept-and-declare, none refused: the startedAt ordering guard, the abandoned-surface carve-out, and lineage on `GET /sessions`. Two asked-but-unbuilt: the launcher half of item 1, which this session settled as delivered (this very session's process carries `CHANNEL_LINEAGE=DEV-DISCORD`), and a one-pass window where a rebind whose thread has no id yet drops its notice.
Findings disposed this round: one Critical and one functional Major, both confirmed by reading the cited code rather than taken on the reviewer's word.
  - Critical: the lineage takeover carried no credential and no liveness check. Lineage is published by `GET /sessions`, `startedAt` is broker-stamped at registration so a fresh claimant is always newer, and the impostor guard keys on a session id some record already holds, so a claimant announcing a fresh id never meets it. Any local process reaching the intake could take a lineage's thread and receive the operator's steering in it.
  - Major: the takeover composed the sticky title from the arriving session's own (absent) entry, so every takeover wrote null over the thread's rename and persisted it, contradicting docs/architecture.md's stated guarantee.
Fix shipped (Section 6, this round): the sticky title is now composed from the entry the takeover resolved; a `hasLiveRelay` callback refuses a takeover while the incumbent holds an attached relay pipe; takeover, refusal and restart-notice paths all log; docs/security-model.md gains a section describing the lineage route, its guard and its residual. The load-bearing stamp-then-persist ordering is preserved and its four pinning tests pass unchanged.
Consult: a fresh-context consultant tested the framing and overturned two of it. `Local only` is not a mitigation, because local processes are this model's entire scope and a subprocess of the supervisor's own child inherits the lineage. And a defense does land in this repository alone, so no BLOCKED was owed. Its ruling rested on one inferred ordering fact, which was then confirmed against the live broker log: old pipe closes 04:43:49.525Z, session ends 04:44:13.497Z, replacement registers 04:45:17.6Z, an 88-second margin, so the guard never refuses a legitimate restart.
Operator decision (decided 2026-09-20): the restart-gap residual is accepted as the interim state rather than switching the lineage takeover off, since the alternative reopens one new thread per restart. The full remedy is handed off to docs/plans/channels_lineage-takeover-credential_spec_v1.md, written this round, whose load-bearing point is that the supervisor must send the secret before the broker requires it.
Assumptions: none.
Gate: this repository's own, re-run by the orchestrator rather than accepted from the implementer - `npm run lint` (`tsc --noEmit`) exit 0, `npm test` exit 0, 1721 tests, 1720 pass, 1 pre-existing skip, 0 fail. Baseline before this round on the same tree was 1717 tests, 1716 pass, 1 skip, 0 fail, exit 0. Delta +4 tests, +4 pass, 0 fail then and 0 fail now. Both fixes proven red-then-green, the liveness guard additionally by a logic mutation (`attached(record.processToken)` to `attached(sessionId)`) restored from a pre-probe copy and verified byte-identical by diff. Contention lane: the machine's heavy-process claim was held across every suite run.
Next: the reviewer pair over this fix delta, then the Minors pass, then documentation curation and close.
Commit Model: Branch-and-PR

### Interim board 2 - 2026-09-20
Pass: finishing-work over this plan, resumed in a successor session after the session holding the leash died mid-gate. No section closed, so this is a board entry rather than a Chapter.
Continuity: the predecessor session ended at 05:44:06Z with the round 2 fix delta uncommitted and its gate unrun. Nothing was lost: the delta is intact in the worktree against `0097af3` and this entry records the state it died in. The operator re-armed the three-plan queue onto this session.
Round 3 (adversarial lens alone at fable, Agent tool, the decayed-round roster since round 2 carried no Critical): CHANGES_REQUIRED, three Majors, four Minors. Every Major was confirmed against the cited code by the orchestrator rather than taken on the lens's word.
  - Major, confirmed: the reconnect grace does not close the broker-restart case it was added for. `pending` is built empty at relays.ts:118 and cleared at relays.ts:220, and nothing rebuilds it, so after a restart every token reads unheld until its own relay returns. The code comment at index.ts:703-705 implied the grace covered it.
  - Major, confirmed: docs/security-model.md misstated what a claimant keeps. The text said "until the next legitimate restart" for every claimant. The truth splits: a pipeless claimant holds the thread but receives no steering, because `deliver` returns false with no attachment (relays.ts:194), while a pipe-holding claimant holds it indefinitely and is protected by this very guard against the legitimate replacement.
  - Major, confirmed: the same-process-token exemption added for /clear is reachable by any token holder. CHANNEL_PROCESS_TOKEN is inherited by every subprocess (registry.ts:520, intake.ts:939), `subprocessStart` declines only `source === "startup"` (registry.ts:531), so a subprocess announcing any other source registers as a replacement and passes the guard while the real session runs. The prior round's guard refused that claim.
Fixes applied this round, all prose and comment corrections changing no executable line: index.ts's liveRelayCheck header now states the exemption's true reach and that the grace covers in-run drops only; security-model.md now states the exemption's reach, both branches of what a claimant keeps, and the measured 88 second restart window rather than the 30 second backoff ceiling.
Operator decision (decided 2026-09-20): Option B. The broker-restart window stays open rather than adding a hold after broker start, on the operator's word on the relay channel. So the window is documented as a residual and not closed here. The full remedy remains docs/plans/channels_lineage-takeover-credential_spec_v1.md.
Convergence check, and the reason this entry exists: the rounds are not narrowing, and the findings move between passages rather than repeating a class. A controlled check over the plan's approval-scoped region (lines 1 to 39) with the predicate livenes|relay pipe|takeover.*refus|refus.*takeover|credential|hasLiveRelay|grace|attached|impostor.*lineage returns one match, item 4, which states the opposite of a standard: that this feature touches no guard. The control, `lineage`, withheld from that predicate, returns 7 hits over the same region with the same instrument, so the region is readable and the near-silence is real. The region does name CHANNEL_PROCESS_TOKEN at lines 20 and 22, as a parallel env var for declaring a lineage and never as a guard. So the guard under review has no acceptance criterion in the approved plan, and reviewers are unbounded in the cases they may raise against it.
Live dispatch: the consultant, at fable, asked to test that framing and to rule between ratifying a modest standard for an interim guard, removing the guard and documenting the hole, or continuing to patch per round. Its ruling is what the next step turns on.
Review-round backstop: this is round 3 of the opening bound of 5. Not fired.
Assumptions: none.
Gate: not re-run this round. The machine's heavy-process claim is held by the dead predecessor session (`Session: 92d60286`, written 05:44:06Z with a 1500 second bound), so it reads live until 06:09:06Z and the protocol waits on a claim inside its declared duration. The round 3 delta changes no executable line, so the standing baseline is unchanged: 1721 tests, 1720 pass, 1 pre-existing skip, 0 fail, exit 0, measured by the predecessor at 05:43Z on SCOTT-CLAUDE against this same worktree. The gate is owed before this pass closes and is named here as owed rather than run.
Next: adjudicate the consultant's ruling, then the gate, then the Minors close pass, then documentation curation and close.
Commit Model: Branch-and-PR

### Interim board 3 - 2026-09-20
Pass: finishing-work over this plan, continuing in the same session. No section closed, so this is a board entry rather than a Chapter.
Rulings adopted since the last boundary: the consultant ruled against removing the guard and against continuing to patch per round. It found the rounds were converging rather than stalling, code defects in round 2 giving way to prose defects in round 3, so the remedy was to ratify a standard and close against it. The operator ratified that standard on the relay channel and accepted its stated risks. It shipped as roadmap item 6, five guarantees and three explicit non-claims, committed in dbe61ba.
Correction to Interim board 2: that entry records the round 3 fix as writing "the measured 88 second restart window" into docs/security-model.md. That figure was wrong and is no longer in the file. The 88 seconds measured a supervisor restart, an old pipe closing at 04:43:49 and a replacement registering at 04:45:17, not a broker restart. The broker-restart evidence is different and weaker: the broker came up at 04:34:44Z and sessions registered at 04:35:37Z to 04:35:40Z, which is registration rather than relay re-attach. The figure was removed rather than moved, and the residual window is now stated without a number.
Round 4 (adversarial lens alone at opus, high effort, through Workflow, the decayed-round roster since round 3 carried no Critical): CHANGES_REQUIRED, two Majors, six Minors, no Critical. It was asked exactly two questions against the ratified standard, whether the code does guarantees G1 to G5 and whether shipped prose claims anything outside G1 to G5 and non-claims N1 to N3, with a scope bar routing any "the guard should also stop X" finding to the credential plan. It confirmed all five guarantees met in code, each against the cited line. Both Majors were prose and both were confirmed by the orchestrator against the code before any fix. Both are fix-introduced rather than spec-traceable: each sits on a + line of this section own delta against 0097af3. First-turn reading was healthy at 32 non-synthetic assistant lines and 0 synthetic, all claude-opus-5.
  - Major, confirmed, fix-introduced: surface.ts and security-model.md both said a supervisor restart "never" meets the guard. False. graceMs is config.relayHeartbeatMs, 15 seconds by default with a 20 second ceiling (RELAY_READ_TIMEOUT_MS is 60 seconds, config.ts:196 and :225), and reapPending only clears a pending entry on a heartbeat tick, so a replacement registering within about two heartbeats of the old pipe closing is refused and logged, then takes the thread on a later pass. Both carriers now state that margin. This is the defect minors item 19 had recorded as a Minor; round 4 raised it to Major because a claim on an authorization boundary is held to the behavior bar.
  - Major, confirmed, fix-introduced: the hasLiveRelay option comment at surface.ts:83-84 described the exemption as a /clear succeeding itself, which is narrower than non-claim N2. The comment now states the exemption is as wide as the process token, which every subprocess inherits, and points at liveRelayCheck as its sibling at :553 already did.
  - One round 4 Minor rode with the Major fix rather than waiting for the close pass, because it sat in the paragraph being rewritten: the security model said the restart notice records the thread or the failure, where index.ts:1205 returns before posting and before logging when no thread exists yet. The doc now names that third outcome.
Round 5, dispatched and in flight: one adversarial lens at opus, high effort, through Workflow, over the round 4 fix delta. The delta changes no executable line, confirmed by a predicate for non-comment changed lines that returns nothing over it and speaks three lines over a known code change in relays.ts. The round is owed rather than optional even so, because the fix-delta bar third trigger names authorization surfaces and this delta touches the takeover guard stated contract and docs/security-model.md. First-turn reading healthy at 14 non-synthetic assistant lines, 0 synthetic, all claude-opus-5.
Review-round backstop: this is round 4 of the opening bound of 5. Not fired.
Fourth plan, received and held rather than armed: docs/plans/channels_stale-after-restart_spec_v1.md, offered by the coordinator persona for the queue behind the current three. Two independent gates hold it, and clearing one would not clear the other. First, authority. The plan carries no Dispatch Authorization section, confirmed by a grep over the whole file whose withheld control, the same pattern run over docs/ in the same tree, named five archived plans that do carry the word. So the operator's approval is reported by the coordinator and is not traceable in git, and confirmation has been routed to the operator. Second, reachability. The plan lives only on origin/plans/stale-after-restart at 8278766 and is absent from this worktree, so the arming CLI would refuse the path today. The reply sent to the coordinator was received-verified-holding-for-authority.
Assumptions: none.
Gate: not re-run this round, the round 4 delta being nil so far. The standing baseline is this session's own close gate, run unclaimed with contention named: npm run lint exit 0, npm test exit 0, 1725 tests, 1724 pass, 1 pre-existing skip, 0 fail, both codes read from the run's own exit-marker files rather than from a grep over output. Measured at 06:12Z on SCOTT-CLAUDE against this worktree at dbe61ba. Against the prior baseline of 1721 tests and 1720 pass, that is +4 tests, +4 pass, 0 fail then and 0 fail now.
Branch state: dbe61ba and 127b8a0 are committed locally and not on origin. A push begun earlier has never landed. git ls-remote exits 0 against origin, so the remote is reachable and authenticated, but it lists no rebind-live-proof branch. Resolving that push is part of the close, before the pull request is opened.
Next: adjudicate round 4, then the Minors close pass over all 20 items in the scratch list, then the close gate, then the closing Chapter, then the push and the pull request.
Commit Model: Branch-and-PR

### Interim board 4 - 2026-09-20
Pass: finishing-work over this plan. No section closed, so this is a board entry rather than a Chapter. This entry exists because the review-round backstop fired, and the backstop requires an owed-and-unrun round to be written to the plan doc rather than left in the declaration body.
Round 5 (adversarial lens alone at opus, high effort, through Workflow, over the round 4 fix delta): CHANGES_REQUIRED, two Majors, three Minors, no Critical. Both Majors were confirmed against the cited code by the orchestrator before any fix was written. Both are fix-introduced: each sits on a + line of this pass's own delta against `0097af3`, established by reading the captured round diffs rather than by recollection.
  - Major, confirmed, fix-introduced by round 3's fix (it is already a + line in the round 4 capture, so it predates round 4): the takeover guard's comment in broker/discord/surface.ts said an ended lifecycle derives to exited "so the one check covers both", which reads as though the exited check at surface.ts:541 catches every claimant that has stopped running. It does not. `deriveSurfaceState` in broker/discord/state.ts returns `exited` for a lifecycle of `ended` (:163), or for a stale record silent past `exitedAfterMs` (:167-169), four hours by default (broker/discord/config.ts:36). A claimant that never held a relay pipe is never ended by a pipe close, so inside that horizon it renders `idle` and passes. The comment now bounds the reach to those two shapes and points at the security model for the residual. This is a claim on the authorization guard, so it is a security Major.
  - Major, confirmed, fix-introduced by round 4's fix: docs/security-model.md said a thread changing hands "leaves a trace outside Discord in every case but that one". False, and written by this pass one round earlier. The takeover log line at surface.ts:614-618 is unconditional and names the thread as not opened yet when there is no id; only the Discord-side notice is missing in that case. The sentence now says so.
Carve-out, stated precisely because the two Majors do not sit on the same footing. The backstop freezes the stopping round's fixes except a Critical and a security finding of Critical or Major weight. The first Major above is such a finding and was rightly fixed ahead of the declaration. The second is not: its subject is logging, which is not on the claim-class block's security-boundary list. It was held to the behavior bar by the published-contract exception instead, contradicting G5 of the ratified standard. So the backstop freezes it. It was nonetheless applied before the freeze and it stays applied, because reverting it would restore a false sentence to a shipped document to satisfy a bookkeeping rule. The operator's continue covers it. Both fixes are prose only and change no executable line.
Review-round backstop: FIRED. This is round 5, the opening bound, with the terminal condition unmet at this round's adjudication. The ladder stage is the opening bound firing for the first time. A continue buys three further rounds, and each adjudication from the third of those onward that still leaves the condition unmet declares again. The count as restarted is zero, restarting at the operator's answer.
Owed-and-unrun round: the two fixes above are a prose-only delta on broker/discord/surface.ts and docs/security-model.md. That delta owes a round. Under the fix-delta bar's third trigger it owes one because the delta reaches the takeover guard's stated contract and the security model. Under the below-bar judgment it owes one independently, because this exact class of delta, a rewrite of the security model's residual paragraph, introduced a false sentence one round ago, and no test reads prose. The prose-deletions clause exempts neither, because both fixes replace a false sentence with a true one rather than deleting one. The backstop opens no round, so that round is named here as owed and unrun, and it is taken first on the re-arm before anything else.
Ruling adopted since the last boundary: the pre-BLOCKED consultant, at fable and high effort, tested the framing this session brought to it and overturned two halves of it. First, the loop is not structurally non-terminating. The terminal condition ends the loop at the first round leaving no owed Major whose fix delta owes no round, and nothing in the rules makes a round produce a Major. Three consecutive rounds finding prose is an empirical run, not a structural bar. What was actually reached is the five-round bound, which is a designed decision ask that restarts on a continue rather than a signal that something must change. Second, the ambiguity in the third trigger over whether a prose-only delta reaches a surface is real kit friction, but it decides nothing here, since the below-bar judgment reaches the same answer on this delta's own history. A kaizen note recording that ambiguity was filed to the kit's inbox this turn.
Residual disclosure, not an ask. Guarantee G3 says a claimant whose own state is exited takes nothing over. The code does literally that, so G3 as ratified is met and no test change is owed; the regression test builds its dead claimant with `lifecycle: "ended"` (broker/discord/surface.test.ts:1726), which is the shape G3 names. The question this session raised was whether non-claim N1 needs widening to name a dead claimant that never held a pipe and renders idle inside the four-hour horizon. It does not, and the orchestrator confirmed that on its own surface by reading the takeover loop in order rather than taking the consultant's word. Such a claimant passes the exited check at surface.ts:541, but must still be newer than the incumbent (:548) and must still find `hasLiveRelay(incumbent)` false (:565). That last condition is exactly the window N1 already states, so the case is a subset of N1 rather than a gap beside it. It also self-heals: the real replacement is newer still and the dead claimant holds no pipe, so the replacement takes the thread on its first pass. Item 6 is therefore not edited and the standard stands as ratified.
Live dispatches: none in flight. The consultant returned and its ruling is recorded above. The pre-BLOCKED expert ask to the live ARCHITECT seat is unanswered and never gates; a late answer is recorded beside the ruling rather than displacing it.
Assumptions: none.
Gate: not re-run this round. The round 5 delta is prose only, confirmed by reading both hunks rather than by a predicate over the diff. The standing baseline is unchanged and is this session's own close gate: `npm run lint` exit 0, `npm test` exit 0, 1725 tests, 1724 pass, 1 pre-existing skip, 0 fail, both codes read from the run's own exit-marker files rather than from a grep over output, measured at 06:12Z on SCOTT-CLAUDE against this worktree at `dbe61ba`. The close gate is owed before this pass closes and is named here as owed rather than run.
Next: the owed round over the round 5 fix delta, then the Minors close pass over the scratch list, then the close gate, then the closing Chapter, the push and the pull request. All of it waits on the operator's answer to the declaration.
Commit Model: Branch-and-PR

### Chapter 6 - 2026-09-20 (close-out)
Completed: nothing further was built. The operator shelved the plan, and this Chapter is the whole of the close-out.
Implemented By: main session (lineage `DEV-DISCORD`)
Metrics: review rounds 0; provenance 0; rulings 0 refused, 0 declared, 1 asked and answered; NEEDS_CONTEXT count 0; escalations 1 (the review-round backstop's decision ask); consults 0 this round.

Operator decision (decided 2026-09-20): shelve the plan, delete the work branch, archive the plan only. The stated ground is that the feature works in production as it stands, and that a local desktop-only virtual machine in a sandbox does not warrant closing edge-case security holes. On the takeover specifically: a conflated lineage name would surface as a session visibly confused by the steering it received, which the operator would notice.

What is in production, and stays. Roadmap items 1 through 5 shipped and merged as PR #1 (`1d406c6`). A session that sets `CHANNEL_LINEAGE` rebinds to the thread its lineage already owns instead of opening a new one, and the supervisor's restarts hold one continuous thread. Chapter 5 recorded that proof read out of the live persisted bindings rather than out of a test. That behavior is untouched by this close-out.

What was built and is now discarded. Branch `rebind-live-proof` held six commits that were never merged, tip `a4f7101`. Its substance was roadmap item 6, a hardening pass over the lineage takeover: a liveness guard refusing a takeover while the incumbent still holds an attached relay pipe, a fix composing the sticky thread title from the resolved entry rather than the arriving one, logging on the takeover, refusal and restart-notice paths, and a `docs/security-model.md` section describing the lineage route. Four added tests came with it. None of that reaches `main`. Deleting the branch is what the operator asked for, and the objects survive only until git garbage-collects them, so there is no rollback past that point.

What stays unfixed, named so a later reader does not rediscover it as new. Five real defects were found by the finishing reviews and never fixed, because the Minors pass they were queued for never ran. `broker/bindings.ts` admits NaN, Infinity and negative values where a sibling validator in `broker/persistence.ts` exists for exactly that check. `hooks/session-start.ps1` gates on `$`, which in .NET regular expressions matches before a trailing newline, so a value ending in a newline passes the gate and then throws, and the surrounding catch swallows the whole announcement. The lineage string is unbounded at intake, where the sibling `name` field is bounded. The lineage re-enters through a cleaner that permits a lone surrogate, which then reaches a Discord request body. One surface write at shutdown is not folded into the in-flight set, so shutdown does not wait for it. The full twenty-item list, including the test-coverage gaps and the journey-narration comments, was scratch-only and dies with this close-out.

Decisions / Surprises: the plan header named a work branch, `passive-supervisor-rebind`, that was never cut. The work ran on `rebind-live-proof` throughout. The header is corrected above rather than left to mislead a reader of the archive. Separately, the pre-BLOCKED expert ask to the `ARCHITECT` seat is recorded in Interim board 4 as unanswered. It was never unanswered; it was never delivered. The persona plugin's `session.receive` hook consumes a peer message before the model reads it, in every armed persona session, while the sender's call still reports success.

Assumptions: the two plan docs are archived together rather than one alone. The operator's words were "archive the plan only", read as archive the plan and build nothing further, not as archive exactly one file. The remedy plan existed only on the deleted branch, so leaving it there would have destroyed it silently, which is the same failure this repository already suffered once with the DSH bridge plan.

Review Findings: none this round; no code changed.
Stamps: none.
Gate: this repository's own, run over the archival delta because tests here read the docs tree. Counts and exit codes are recorded in the commit that carries this Chapter.
Next: none. The plan is shelved and archived.
Commit Model: Branch-and-PR
